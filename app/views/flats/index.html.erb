<%= render "components/banner_search" %>

<% if current_user %>
  <%= link_to "Sign Out", destroy_user_session_path, data: {turbo_method: :delete} %>
<% else %>
  <%= link_to "Sign In", new_user_session_path %>
  <%= link_to  "Sign Up", new_user_registration_path %>
<% end %>

<% if current_user %>
  <%= link_to  "User Account", user_path(current_user) %>
<% end %>
<%= link_to "New", new_flat_path, class: "btn-flat m-2" %><br>
<div class="container m-5">
<%# <% if current_user %>
<%# <%= link_to "Sign Out", destroy_user_session_path, data: {turbo_method: :delete}, class: "btn-flat" %>
<%# <% else %>
<%# <%= link_to "Sign In", new_user_session_path, class: "btn-flat" %>
<%# <%= link_to  "Sign Up", new_user_registration_path, class: "btn-flat" %>
<%# <% end %>


  <div class="cards">
    <% @flats.each do |flat| %>
    <%= render "/components/card_flat_vertical", flat: flat %>
    <% end %>
  </div>

 <%# <%= link_to flat.name, flat_path(flat) %>
  <%# <%= flat.street %>
  <%# <%= flat.zipcode %>
  <%# <%= flat.city %>
  <%# <%= flat.full_address %>
  <%# <%= flat.description %>
  <%# <%= flat.price %>
  <%# <%= flat.mean_rating %>

</div>
