<h1>Your Dashboard</h1>

<div>
  <%# <%= link_to 'Homepage', root_path, class:'btn btn-primary' %>
</div>

<%= render '/components/description_user' %>
<%= link_to "Update profile", edit_user_registration_path %>

<div class="container  my-3">
  <h2>My bookings</h2>
  <% @bookings.each do |booking| %>
    <p>start date: <%= booking.start_date %></p>
    <p>end date: <%= booking.end_date %></p>
    <%= link_to 'add a Review to this booking', new_booking_flat_review_path(booking) %>
  <% end %>
</div>

<div class="container my-3">
  <h2>My flats</h2>
    <%# si (il a un booking ET que j'ai laissé une review) OU (il n'a pas de booking) %>
    <%= render '/components/card_flat_horizontal', partial: @flats %>
    <%# Sinon on affiche avec un bouton review %>
  <%# <%= link_to 'add a Review to the tenant', new_user_user_review_path(flat.user_id) %>

</div>
